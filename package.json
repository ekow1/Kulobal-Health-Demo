{
  "name": "kulobal-health-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd api && npm run dev",
    "build": "echo 'JavaScript backend - no build required'",
    "start": "cd api && npm run start",
    "install": "cd api && npm ci",
    "test": "echo \"No tests specified\" && exit 0"
  }
}
